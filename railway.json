{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/frontend/artifacts/api-server && pnpm install && pnpm run build"
  },
  "deploy": {
    "startCommand": "cd apps/frontend/artifacts/api-server && pnpm run start"
  }
}
