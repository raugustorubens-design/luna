# luna

> ⚠️ **Este repositório não é mais o destino de desenvolvimento ativo para a maior parte do ecossistema LUNA.** Partes significativas do código aqui foram portadas (ADR-004, ADR-012) para repositórios próprios e continuam presentes neste monorepo apenas como histórico — sem aviso visível na maioria dos casos, o que já causou trabalho aplicado no lugar errado mais de uma vez.
>
> - **Backend real** (Gateway, Cognitive Engine, `/api/chat`, `/api/context`, Convergia): `raugustorubens-design/luna-core`. `apps/frontend/artifacts/api-server/` aqui é a cópia pré-porte, superada.
> - **Frontend/Forge real** (Dev Mode, `/forge`): `raugustorubens-design/luna-frontend`. `forge/` aqui é a cópia pré-porte, superada (ver `forge/README.md`).
>
> **Antes de editar qualquer arquivo neste repositório**, confirme que o caminho não tem equivalente já portado em um desses dois repositórios — muitos arquivos aqui têm comentário de cabeçalho começando com "Porte de ..." no destino, mas nenhum aviso equivalente na origem.